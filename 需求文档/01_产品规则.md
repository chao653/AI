# 01 · 产品规则（已确认清单）

> 只收**已确认**的业务规则；状态为「待确认」的先放 `待确认问题清单.md`，确认后再搬进来。
> 决策顺序中本文件排第三（用户当前指令 > 用户指定节点/PRD 范围 > 本文件 > 本地执行文档与走查结论 > 通用判断）。

| # | 规则 | 来源 | 状态 |
| --- | --- | --- | --- |
| R1 | 待填 | 待填（PRD 锚点 / 用户口头 / Linear 评论） | 已确认 / 需用户确认 |

## 记录约定

- 来源写到可回溯的粒度：PRD 锚点编号（见 `00_PRD溯源.md`）、Linear 工单号、或 `决策日志.md` 日期。
- 规则被推翻时不删行，状态改为「已废弃」并在 `决策日志.md` 加 `[修订]` 条目。
