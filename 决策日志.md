# 决策日志（AI）

> 全项目拍板记录：产品口径与设计执行的决策都记这里。
>
> **约定**
> - 新决策**置顶**追加（最新一条永远在最上面）。
> - 推翻旧决策不删原文，新增一条 `[修订]` 条目并指向被推翻的条目。
> - 新会话先读顶部 5 条。
> - 每条格式：`## YYYY-MM-DD · 主题` + 决策内容 / 依据 / 影响范围 / 状态（已确认 / 设计判断 / 需用户确认）。

---

## 2026-09-09 · [修订] 优化一律出独立设计稿，不改原稿

- **决策**：页面优化以「克隆原帧 → 新帧命名 `optimization N`」方式产出，与用户既有 `optimization 2 / 3` 同行摆放并加注释文本；原帧保持原样。本次原帧 `3420:56555` 已全量回退，产出 `optimization 4`（`3534:23922`）。
- **依据**：用户指令「不是在原稿上面看，是另外做一个设计稿」。
- **影响范围**：推翻下一条「答题页首轮优化口径」中「对 3420:56555 做修正」的做法；修正内容本身保留，转移到新帧。
- **状态**：已确认。

## 2026-09-09 · 答题页 General Passage · highlighted 帧首轮优化口径

- **决策**：对 `3420:56555` 做规范合规修正（题号同步、高亮收口、stepper auto layout、语义命名）并把示例题目改为与文章匹配的阅读理解题；库侧问题与同组两帧同步不在本轮动。
- **依据**：用户指令「帮我优化一下这个页面」；`走查/R1_报告.md`。
- **影响范围**：仅该帧；Question Card 原文案已存日志可回退。
- **状态**：结构修正 已确认；题目内容替换 设计判断；同组帧同步 / 底部硬切处理 / Notes Switch 文案 需用户确认。
- **项目信息补充**：Figma 文件 `Assessment-2.5 - General Passage`（fileKey `m44q0l36tqh9l1PdggbGho`）；产品范围疑为学生端测评答题流程，待用户确认后回填 `AGENTS.md` 与 `.cursor/rules/ai.mdc`。

## 2026-09-09 · 项目初始化

- **决策**：按 `project-scaffold` 脚手架创建 AI 项目标准目录结构；项目名取仓库名 `AI`。
- **依据**：用户指令「开始新项目」。
- **影响范围**：全部目录与模板文件；`设计执行/规范/` 八份文档整套复制自脚手架快照（来源 Kira_QuickGrader，2026-08-11 打包）。
- **状态**：已确认（结构）；三库口径（组件 `ShadCN-Default` / token `Design System 2.0 Foundations` / 图标 `Design System 2.0 Icons`）暂沿用 Kira 默认，**需用户确认**。
